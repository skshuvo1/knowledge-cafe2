import React from 'react';

const Storage = ({mark}) => {
    // console.log(mark);
    //     localStorage.setItem('name', mark.name);
       
    
    return (
        <div>
            
        </div>
    );
};

export default Storage;