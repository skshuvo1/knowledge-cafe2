import React from 'react';

const List = () => {
    return (
        <div>
            <h2>
                localStorage.getItem('name')
            </h2>
        </div>
    );
};

export default List;